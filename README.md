# AUTH

An authentication service demonstrating authentication in Rust.

## Getting Started

To get started with AUTH, follow these steps:

1. Clone the repository: `git clone https://github.com//auth.git`
2. Install dependencies: `cargo install`
3. Run the application: `cargo run`

## Tech Stack
- Rust
- Axum
